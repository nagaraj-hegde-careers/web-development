//----------x----------x----------x----------x----------x----------//
// filter fuction is used to filter out elements from an array based on a condition.
// It returns a new array containing only the elements that satisfy the condition specified in the callback function.

// example 1: filtering even numbers from an array
// let arr = [1, 2, 5, 7, 8, 2, 6, 9, 13, 17];
// let evenNumbers = arr.filter(function(element){
//     return element % 2 === 0;
// })
// console.log("Even Numbers:", evenNumbers); // Output: [2, 8, 2, 6]

// example 2: filtering positive numbers from an array
// const transactions = [1000, 3000, 4000, 2000, - 898, 3800, -4500];
// let positives = transactions.filter(function(element){
//     return element > 0;
// })
// console.log("Positive Numbers:", positives);
//----------x----------x----------x----------x----------x----------//
